# User Trace Authentication Service

## Before Using

you will need to install the AWS CLI and have successfully run the `aws configure` command:

https://docs.aws.amazon.com/cli/latest/userguide/getting-started-install.html
